<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">PokeWeather</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= weather_path %>">Weather</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= pokemon_path %>">Pokemons</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= history_path %>">History</a>
      </li>

      <!-- User Authentication Links -->
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "Edit Profile", edit_user_registration_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-link nav-link" %>
        </li>
      <% else %>
        <li class="nav-item d-flex">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
        </li>
      <% end %>

      <% if user_signed_in? %>
        <li class="nav-item">
          <div class="nav-item-email-container">
            <span class="nav-item-email text-white"><%= current_user.email %></span>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>